<rule-template duration="5000" type="range-rule">
	<model-param param="report,domain,type,name,totalCount,max,min,avg,tps" />
	<query-param param="report,domain,type,name" />
	<match extendParam="min,avg,max"
		method="
		    def counter = 0;
		    models.each{
		        def model = it.getValue();
		        counter += model.getData('avg');
		    }
		    if(counter>=min && counter<avg){
		        return 0;
		    }
		    if(counter>=avg && counter<max){
		        return 1;
		    }
		    if(counter>=max){
		        return 2;
		    }
		    return -1;
		   " />
</rule-template>
